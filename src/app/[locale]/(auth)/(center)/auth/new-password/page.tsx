import NewPasswordForm from '@/components/screens/auth/new-password-form';

export const metadata = {
  title: 'New Password',
};

export default function NewPasswordPage() {
  return <NewPasswordForm />;
}
